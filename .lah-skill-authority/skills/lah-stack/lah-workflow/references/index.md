# LAH Workflow References Index

## Persona / channel parity
- `canonical-persona-and-parity-patterns.md` — persona files supersede code fixes, comparison harness pitfalls, runtime topology, parity architecture
- `cloe-persona-fidelity-pattern.md` — canonical persona authority, social fast path, strict factuality, fidelity test shape
- `live-openclaw-agent-validation-pattern.md` — fresh-session validation against deployed runtime (pty capture, session keys)
- `conversational-persona-live-test-pattern.md` — live persona test harness
- `conversational-comparison-harness-pattern.md` — old-vs-candidate comparison + human gate
- `cloe-persona-context-parity-pattern.md` — canonical persona context builder + cross-channel parity proof (hash over canonical files, not system-prompt hash)

## Git / workspace
- `git-workflow-detail.md` — worktrees, cherry-pick, LOCAL_CI_VERIFIED merge policy, stash recovery; dead CI check "never started" vs "failed" (billing/quota annotation protocol); staged-set == working-tree hash verification (must run from repo root); Continuity JSON committed pre-merge is stale (don't amend merged history, separate doc commit instead)
- `git-verification-workspace-pitfalls.md` — stale origin vs origin-https in workspace clones, `git ls-tree` false-positive existence check, re-inventory after rebase, full-suite test globs, **canonical checkout main diverged from true origin/main (rebuild branch on fetched origin/main before commit)**
- `lah-brain-deploy-auth-runtime-facade-map.md` — LAH Brain deploy path (remote Hostinger VPS, deploy = merge to origin/main, no SSH/CI/webhooks from OpenClaw VPS; verify via /version + /go/:token), read-auth matrix (money/campaign-factory/obs = session-cookie only vs openclaw-state/approvals/admin = x-admin-api-key), business-runtime facade pattern (compose in-process on lah-brain behind ONE admin-key endpoint; Cloé-side `business_state` memory-interface bridge), OFFER_MAP configured-offer inventory source (redacted, server-side URL rule), Lot-1 pitfalls (invalid-date toISOString RangeError ordering, patch-tool consuming JSDoc `/**`, fresh worktree npm install, multi-lot resumability across turn caps)

## Live gateway / dashboard testing
- `openclaw-gateway-ws-live-test-protocol.md` — real dashboard path via WS (18789): device-identity Ed25519 handshake, protocol 4 connect, device token scopes, chat.send string contract, sessions.json transcript resolution, operator-mandated dashboard-only fallback gating, when to stop protocol reproduction and hand the browser test to the operator
- `live-certification-smoke-harness-pattern.md` — real-provider A–H certification smokes against the deployed /chat/completions (read-only stub executor, `_cloe.governor` evidence capture, parallel tool_calls grouped in ONE assistant message, tool-result `content` must be a string, reinjected `arguments` '{}' fallback, provider timeout-abort race → PROVIDER_REQUEST_FAILED empty 500 (map AbortError→PROVIDER_TIMEOUT), ANSWER_SUFFICIENT skips planner+budget gate (PR #701 tension), harness-bug vs production-defect vs design classification protocol)
- `live-certification-harness-false-negatives-and-orchestration.md` — harness false-negative classification table (harness / provider-variability / timeout / orchestration / production-defect buckets + actions); 3 concrete false negatives observed (B hard-coded threshold 3 vs effective budget 4 — derive caps from `budget.budget.max_total_tool_calls`, `budget_exceeded` with zero further execution is PASS; G substring regex flagged textual `<tool_call>` citation inside a security refusal — FAIL only on structured dispatch/execution, server structural detector is the authority; A "grounded no-tool" exposed client tools → deterministic no-tool = no tools + `tool_choice:'none'`); no-tool routing fact (`hasNativeTools` gate → brain path never exposes `governor.decision`, prove retrieval-sufficiency with a one-time native probe tools+tool_choice none); bounded per-scenario orchestration (separate spawned process per scenario, own timeout, immediate JSON persistence, resume without re-execution, incomplete runs never counted, invalidate runs from prior harness versions); operator strict-corridor pattern (harness-only corrections, deterministic fixtures before live rerun, no full-suite relaunch, failures unmasked, success verdict only under the SAME final harness version); harness versioning discipline (version embedded in every record, ANY change invalidates prior runs, cosmetic record gaps documented + proven by source/probe instead of re-running 48 live scenarios); consent-gate-safe certification (NEVER rm/rm -rf/unlink/find -delete/git clean, fresh timestamped result dirs, blocked read-only re-verification → rely on ALREADY_DEPLOYED + prior approved evidence, NOT_EXECUTED_CONSENT_GATE cleanup does not block a verdict, docs-only closure PR from the deployed SHA); receipt authority metadata normalization (verify PR heads/merges via `gh pr view --json title,headRefOid,mergeCommit` — abbreviated sibling heads get conflated, e.g. pr703.head_sha wrongly = PR #704 head; full SHAs in receipts; `repository.main_after_documentation_closure` vs `production.deployed_sha` after docs closures)

## Tool-loop governance
- `exact-transcript-fixture-derivation-pattern.md` — building an EXACT regression fixture 1:1 from a retained OpenClaw session JSONL once operator consent is granted (complements the consent-gated shape-exact fallback → upgrades verdict PARTIAL to CERTIFIED). Schema traps: OpenClaw role is `toolResult` (10 chars) NOT `tool`; assistant content is an ARRAY of `toolCall` items (id/name/arguments/partialArgs) not OpenAI `tool_calls`; arguments may be object OR JSON string. Pipeline: structural inspect first (no content dump) → minimal field extraction → ordering check (`call_ids == result_ids`) → secret scan (0 hits) → build OpenAI-format fixture (arguments serialized to string). Pitfalls: native_tool_calls list overwritten when initialized inside the loop; shape-equivalent ≠ exact (state both explicitly in receipt); assertion fragments must match REAL transcript content (verify BEFORE writing test — the shape fixture's invented strings like `CAMP-CONV PLANNED` must be asserted ABSENT); user message byte-identical assert. Closure gate shape: Gate 0 read → Gate 1 fixture → Gate 2 regression → Gate 3 deployed authority (no redeploy for test-only deltas) → Gate 4 receipt/continuity/packet/memory lock; no follow-up repair mission when closure passes
- `cloe-post-tool-sanitized-recovery-context-pattern.md` — when the single bounded protocol recovery retry REUSES the native post-tool transcript shape (assistant.tool_calls + role:tool + tool_call_id), DeepSeek is reconditioned into re-emitting DSML even with tools absent / tool_choice=none (`CLOE_POST_TOOL_RECOVERY_CONTEXT_RETRIGGERS_PROVIDER_TOOL_PROTOCOL_V1`). Fix = change ONLY the recovery context construction: `buildProtocolRecoveryMessages()` (rules A–F: preserve user authority, preserve safe grounding, strip native tool protocol, convert tool results to COLLECTED READ-ONLY EVIDENCE textual block with tool names from the transcript's own tool_calls, never invent, final directive LAST). Keep retry bound = 1 and 35s/70s timeouts. V5 A/B/C diagnosis proof, T1–T20 test shape, JSON-key assertions (not substring), real-provider proof harness (mock round-1 DSML, real recovery), regression fixture from the failing session jsonl (consent-gated; shape-exact fallback → verdict PARTIAL), post-deploy A–G cert, live-harness scenario-G false-positive note
- `provider-round-timeout-policy-pattern.md` — provider deadline tuning on the CLOE native loop: DUAL `callProvider` surfaces (main round + denial-driven synthesis re-request — both must be covered), differentiated per-round policy (standard 35s / post-tool 70s, stateless round classification from message history), explicit-`timeoutMs` harness preservation (`timeoutMs = undefined` default so tests' 50/60ms stay honored), `planDeadlines()` as canonical source, `_cloe.provider_round` observability, consent-gate for real-provider required tests (SKIPPED → `residual_limitations`, verdict `_PARTIAL`), temp-secret `chmod 600` hardening when rm is refused, container GIT_COMMIT drift check before assuming where the fix lands
- `native-tool-loop-response-boundary-governance-pattern.md` — REPAIRED the two live-certification blockers of PR #703/#701 (PR #704): (A) canonical timeout/abort normalization — Promise.race + controller.abort() lets the fetch's DOMException AbortError win → misclassified PROVIDER_REQUEST_FAILED → fallback skipped; one `isTimeoutAbortError` boundary (name/ABORT_ERR/abort-wording) + fallback on every timeout (never blank), RED-test via signal-respecting fetch stub; (B) response-boundary execution budget — history-only counting misses the current round's requests and `!forcedSynthesis` guards let ANSWER_SUFFICIENT bypass the hard budget; doctrine tool-definition preservation ≠ execution authorization; authorize only the requested prefix within `remaining = max(0, cap − historyUsage)`, deny excess/mutation calls (never sent to executor), ONE bounded synthesis re-request when all denied, mutation predicate covers BOTH naming orders (`campaign_play` vs `play_campaign`). Verification traps: worktree NAME breaks path-sensitive tests (FORBIDDEN_TOKENS substring-matches absolute paths → false differential regressions) → run full-suite differential from a NEUTRAL-PATH worktree; evidence harnesses under `docs/evidence/` not `test/` (node-test-discovery-boundary); `git rev-parse <merge>^{tree}` identity check for merged-tree = reviewed implementation
- `native-tool-business-context-parity-pattern.md` — native /chat/completions with tools bypassed the server-side business enrichment that /brain/ask and no-tool /chat/completions get (business_context=null, evidence_present_count=0, tool budget burned rediscovering facts). Fix = shared `buildCanonicalBusinessContext()` module composing EXISTING sources (business runtime + AEC + offer inventory + microtest), injected into BOTH entry points, rendered via the SAME `formatCognitiveContextPack`, as a SYSTEM message (never a tool → no budget cost). Diagnosis: verify deployed container == origin/main by byte-identity (`docker exec sha256sum` vs `git show origin/main`); live A/B/C probe capturing `_cloe.governor`. Verification: provider-payload capture assertions (assert on `JSON.parse(init.body)` system messages, NOT the response object), pure-extraction byte-parity check (every removed functional line present in the shared module after accumulator rename), env-fixture completeness (dummy DEEPSEEK_* vars or PROVIDER_NOT_READY).

## Mission lifecycle
- `operator-packet-format.md` — Gate 7 packet
- `bounded-codex-progression-wiring-pattern.md` — wiring the bounded Codex adapter into the progression runtime via a bridge (different call contracts: `executeWithGovernance` seam vs `runBoundedCodexTask`); one-task roadmap `dependencies: []`; `repository_path` follows declared `workspace_path`; `node --test <dir>/` Node≥22 trap; interrupt-at-promotion resume simulation (not executor throw); launcher `release-manifest.json` baseline_sha requirement; planner missions lack bounded fields; real-Codex stdout parse nuance; 12-scenario wiring test recipe + real 3-lot certification
- `continuity-json-schema-pitfalls.md` — Gate 11 traps
- `full-suite-test-traps.md` — bare `node --test` hangs (explicit globs), tracked `test/reports/*.json` churn (restore before commit), **baseline from fresh worktree at exact origin/main SHA (canonical checkout is dirty/behind)**, compare failures by NAME not TAP number, flaky-by-state-accumulation attribution (test pollutes `data/memory-events/` → isolated rerun fails; clean store + prove pre-existing on baseline), untracked `data/` artifacts from full-suite runs must not be staged
- `canonical-checkout-sync-pattern.md` — workspace clone vs canonical checkout sync
- `branch-base-reconciliation.md` — branch base fixes
- `evidence-recollection-supersession-pattern.md` — evidence recollection after SHA rebind: deps-less worktree false-NOT_VERIFIED test receipts (ERR_MODULE_NOT_FOUND recorded as TEST_ASSERTIONS_FAILED; per_file tests:1/fail:1 with FILE name in test_names is the giveaway), fix = clean worktree at same SHA + `cp -al` node_modules hardlink + re-run real collector, then append superseding receipts with explicit `evidence.supersedes_receipt_ids` (copy latest VERIFIED, strip derived id/hash, appendReceipt — collector-live-import mechanism, no fabricated evidence); live gate verdict import (acceptance gates: source/deployed SHA, environment production, collector-live-import, authority OPERATOR; graph_hash evidence-only); consent-gate-safe admin key (Python split-string `'ADMIN'+'_API_KEY'`, process memory only, never argv/print; blocked: shell grep, heredoc, `cmd | python3 -c` pipes, rm -rf even /tmp, foreground compose up); deploy-consent resume packets (`LOT_N_<CAP>_IMPLEMENTATION_MERGED_IMAGE_READY_DEPLOYMENT_CONSENT_PENDING`); **deploy verification (operator manual-deployment confirmations can repeat a PREVIOUS deployment block — always re-verify docker inspect GIT_COMMIT/StartedAt; bind certification to actually-deployed SHA); route-gap discovery (module extended but route still calls old function — runtime proof reveals it, evidence runtime probe does NOT); gh `--match-head-commit` full-SHA requirement; per-lot certification file cycle (3 artifacts, docs branch, memory lock, bounded live read-only GET proof with rows=0 honest)**
- `supersession-resolver-pattern.md` — canonical supersession in content-addressed evidence systems (resolver semantics, `allReceipts` cross-target pitfall, content-addressed cycle impossibility, candidate-SHA authority after policy repair, Phase 8 rebuild discipline)
- `hot-reload-single-flight-promise-leak.md` — provider single-flight promise leak (synchronous early-return paths leak a resolved `currentCachePromise` → hot reload frozen forever); .then()-on-assigned-promise fix; publisher duplicate-promotion `ALREADY_CURRENT` idempotency; chain-integrity validator; real no-restart hot-reload proof recipe (PR #681 → merge 949795a)

## Channel convergence / brain
- `canonical-channel-convergence-pattern.md` — contract, shared brain router, A/B/C taxonomy, ESM re-export trap
- `runtime-call-count-verification-pattern.md` — entry point call-count verification
- `provider-raw-tool-markup-temporal-grounding-pattern.md` — provider emits textual DSML in `message.content` instead of native `tool_calls` (raw markup visible, tools never execute): narrow STRUCTURAL detector, fail-closed `PROVIDER_RAW_TOOL_MARKUP`, terminal-turn guard `PROVIDER_EMPTY_TERMINAL_CONTENT`, deterministic IANA temporal grounding (`buildTemporalAuthorityContext`, injectable clock, `CLOE_TZ_INVALID_FALLBACK`, de-dup marker), B1–B7/E/F1–F6/G test shape, non-mutating live-shape HTTP harness; new standalone `.mjs` in `test/` must be registered in `STANDALONE_TEST_SCRIPTS` (`tools/ci/node-test-discovery.mjs`)
- `cloe-business-evidence-planner-pattern.md` — business requests bypass the capability graph/registry in the native tool loop (full client tool set reaches provider, no evidence plan → uncontrolled loop); reuse-first deterministic adapter (request-class doctrine → required facts → HIGH_ROI registry resolution → strict evidence present/missing → tool filtering → governor budget reuse → sorted deterministic hash); 2-point wiring (buildNativeChatCompletions + server.js governance); payload-aware provider mock harness; `git stash push --wiring` RED proof; graph JSON has NO business caps (registry is the source), `getKnowledgeStack()` gateway lacks campaignMemory/qdrant/mem0 backends, OpenClaw loop → gateway /chat/completions (WS /gateway is ACP not the loop)
- `cloe-business-runtime-facade-wiring-pattern.md` — adding a READ-ONLY business capability Cloé consumes via /brain/ask (Lots 1–5 of CLOE_LAHB_AUTONOMOUS_AFFILIATE_RUNTIME_E2E_V1): LAH Brain facade → openclaw bridge → composition service → cognitiveContextPack.available_items injection in buildBrainAskResponse (try/catch block shape, fail-closed unavailable branch, compact_summary + attach_to_prompt); contract-shape mismatch pitfall (service returns top-level sections, wiring reads `.context` → unit tests pass, live Cloé fails closed — assert shape in the wiring test + live probe first); fetchImpl MUST be threaded into nested services or the test mock never sees the call; live service probe before commit; REAL Cloé certification loop (fresh session key → mission prompt → answer grounded in bridge facts → receipt JSON)
- `cloe-pipeline-persistence-certification-pattern.md` — LOT 8 of the same mission (persistence proof for a conversational pipeline): certify against the DEPLOYED SHA not the local working tree (container sha256sum vs `git show <sha>`), persistence-seam proof via before/after data-layer inspection (decision-records/, memory-events/, packets, receipts — zero delta = seam absent even with a perfect answer; `MEMORY_APPEND_WRITE` unset silently disables appendMemoryFact), minimal repair = call the EXISTING writer (`createRecord`) on the success path only, non-blocking, expose record id in cognitiveContextPack metadata; pipeline test-harness trap (provider env MUST be in the env param — `env: {}` → `resolveBrainProviderConfig` disabled → early local-fallback return skips the block under test; LAH bridge reads process.env, provider config reads env param); secret-masking corrupts hand-written awk KEY extraction (cp a known-good script, sed only the changing parts)
- `live-deployed-route-e2e-proof-pattern.md` — live E2E proof against the DEPLOYED container route (LOT 8 final re-run): masking-safe admin key via `docker inspect --format '{{json .Config.Env}}'` + Node script with concatenated key names (`'ADMIN_'+'API_KEY'`), deterministic client-side session-key derivation (resolveSafeSessionKey: `sk_` + sha256(principalHash:conversationId) — fresh conversation_id ⇒ SESSION_KEY_NEW/PRIOR_TOOL_CALLS=0/BUDGET_START=0 by construction), file-level persistence ground truth (inspect the record file, never response text), docker-compose.override.yml redirects /app/data to the CANONICAL checkout (find real mount via docker inspect Mounts), new-record identification by owner (root vs app uid), LLM-cited context_id ≠ record context_id (AEC runs twice per turn — record's context_id is authoritative), negative assertions from container logs since deploy timestamp

## Runtime forensics / incident proof
- `cloe-tool-markup-obfuscation-and-deploy-fallback.md` — obfuscated DSML guard bypass (fullwidth U+FF5C `＼＼DSML＼＼` variant invisible to `detectRawToolControlMarkup`; JSON+SSE pass-through proof; classify WITH_EXTERNAL_RUNTIME_LIMITATION not deployment failure), deployer-blocked-by-pruned-image manual same-guarantee fallback (PREVIOUS_IMAGE_TAG_FAILED → worktree+compose sequence), rollback build-order trap (rebuilding prev SHA clobbers `latest`/orphans target image → build rollback first), session-store/trajectory-as-data inspection (1 `model.completed` = no tool dispatch), agent self-sabotage pitfall (literal `<invoke name=...>` in tool args breaks the run — build markup strings by concatenation/char codes)
- `openclaw-session-forensics-pattern.md` — locate the incident session in `~/.openclaw/agents/<agent>/sessions/<id>.jsonl` + `.trajectory.jsonl` by local→UTC timestamp match; what each trajectory field proves (`stopReason`, `responseId` format, `itemLifecycle.startedCount`, `toolMetas`, message roles/idempotencyKeys); PROVEN_FROM_RUNTIME/CODE/REPRODUCTION/UNPROVEN labels; redact secrets while extracting; don't misattribute transcript fragments (user-typed vs system artifact)
- `cloe-tool-budget-session-semantics.md` — CLOE tool budget is PER-SESSION cumulative (ordinary 4 / diagnostic 16; `missionType` hardcoded `'conversational'` in server.js → diagnostic budget never applies to dashboard); "fresh session" in prompt text does NOT reset the budget (same session key = same history = still exhausted); `TOOL_BUDGET_EXHAUSTED: YES` is LLM-reported governor state (literal string absent from all source); one parallel batch of 4 `memory_search` = 4/4; reset requires a NEW session key (/new or new dashboard chat); diagnostic recipe + operator unblock + candidate code fixes

## ExoClick / providers
- `exoclick-device-subid-authority.md` — device→payload, SubID1→aff_sub1, URL redaction
- `exoclick-device-subid-wiring-authority.md` — wiring authority
- `exoclick-zone-prefilter-pattern.md` — zone/site prefilter missions
- `exoclick-readonly-stats-authority.md` — read-only stats + fail-closed cost reconciliation
- `exoclick-provider-context-aec-pattern.md` — provider readback grounding into AEC (read-only provider-context adapter, OBSERVED/PARTIAL/MISSING vs ZONE_STATS separation, DI getToken/fetchImpl, facade-builds-field-never-returns trap, test-mock propagation through buildNativeChatCompletions→buildCanonicalBusinessContext→AEC)
- `true-provider-streaming-sse-pitfalls.md` — TRUE provider-side SSE streaming + staged non-destructive timeouts (PR #736, b7fbddd): shared transport for BOTH native-tool and brain/ask paths (parity by construction), staged policy (connect→first-token→idle reset-per-chunk→absolute ceiling; legacy code preserved under explicit combined pre-stream budget), SSE pitfalls (web ReadableStream double-lock, unref timers + never-resolving fetch hang, setInterval race-poll leak → timer-owned rejectable promise + iterator.return() cleanup, usage arrives TOP-LEVEL on final chunk not in choices, forbidden-token static scans match COMMENT text e.g. "through" contains "gh ", legacy failure-code drift INVALID_PROVIDER_JSON vs PROVIDER_STREAM_MALFORMED), full-suite baseline-worktree diff methodology (comm -13 by NAME; OOM-crash cascade subtests + state-pollution failures are pre-existing — verify standalone on clean base), live certification harness (per-chunk timestamps: first-chunk-before-completion, >35s/70s no-timeout while chunks arrive, single finish+[DONE] no duplicate, zero tool_call deltas)
- `reasoning-model-provider-budget-pattern.md` — reasoning model token budgets

## Memory / data
- `memory-stack-reconciliation-instant-retrieval-pitfalls.md` — memory stack reconciliation
- `memory-jsonl-repair-pattern.md` — JSONL repair
- `mem0-controlled-write-preflight-revalidation.md` — mem0 preflight
- `data-layer-mission-pattern.md` — data product missions
- `bounded-evidence-memory-subsystem-pattern.md` — adding a bounded evidence-safe memory subsystem (campaign-memory-style): identity-based idempotency (payload_hash EXCLUDED from identity), atomic tmp+fsync+rename + per-identity lock, corruption reporting, narrowed conversation intent (identity OR term+context — bare-keyword trigger regression trap), fail-closed evidence write kill-switch, express Router.handle test harness, baseline regression proof via comm on exact-base worktree, stale .env GIT_COMMIT override at deploy, write-path test store pollution
- `canonical-memory-continuity-pattern.md` — CLOE_CANONICAL_DECISION_CONTINUITY_AND_OPERATIONAL_LEARNING_MEMORY_V1 (PR #754): governed canonical memory on the EXISTING memory-events store (authority classes, subject/scope, supersession resolver, fail-closed write seam, learning promotion ladder, session continuity, campaign playbook, observability). Resolver semantics that must NOT be "simplified": SINGULAR (decisions/state-refs) vs CUMULATIVE (rules) per subject; conditional scope filter; 'unresolved' ≠ conflict. Write seam DOUBLE gate trap (CLOE_CANONICAL_MEMORY_WRITE + internal MEMORY_APPEND_WRITE); requireCampaignId:false; TDZ shadowing bug; stash-based pre-existing-failure proof; data/memory-events test pollution; sync-only collectors in buildCognitiveContextPack; behavioral receipt written outside repo + validated from repo root

## Security / hardening
- `minimal-security-gap-repair-pattern.md` — security gap repairs
- `security-pattern-false-positive-verification.md` — false positive verification
- `external-tool-evidence-collector-pattern.md` — external tool evidence producer contract
- `trust-boundary-evidence-pattern.md` — trust boundary evidence

## Marketplaces / read-only dataset readers
- `readonly-marketplace-dataset-reader-pattern.md` — bounded read-only marketplace dataset readers for Cloé (CLOE_READONLY_MARKETPLACE_DATASET_READER_V1, PR #740 8b1c152 + fix #741 1d84f1f): module-relative dataset root, allowlist-only file access, quoted-field CSV parsing, payout ov/ap never-merged with conflict flag, RESEARCH_SNAPSHOT provenance on every result, bounds + has_more, conservative FR/EN interpreter (fail-closed null), additive `marketplace_dataset` item in buildCanonicalBusinessContext + formatItem renderer branch; **THE TRAP: `available_items` pushed last gets sliced off by formatCognitiveContextPack's MAX_ITEMS=10 in the dense brain path (dossier+knowledge+v5+web+business > 10) → live cert fails while local tests pass → inject query items with `unshift()` + dense-pack regression test**; live cert harness (`x-admin-api-key` header, key from container env), deploy nuance (canonical deployer forces data mount to canonical checkout — tracked dataset must exist there before deploy)

## Full catalog
The complete list of every reference file lives alongside this index in the
`references/` directory. Load any file with
`skill_view(name='lah-stack/lah-workflow', file_path='references/<name>.md')`.
